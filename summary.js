/*
Fundamentals
1.1 variable (let, const) three types variable
1.2 condition (<, >, ===, !==, <=, >=) && ||, if-else if-else
1.3 Array: Declare, length, index, push, pop, indexOf, includes.
1.4 For loop, while Loop,
1.5 Function Return Parameter,
1.6 Objects property, Including Array, Objects.

=============
====================
ES6
1. Template String ${}.
2.1 Spread(...) copy and array and then add a new Element to and array.
2.2 Use filter to remove an element form a array {!=='phone'}
3. Arrow Function
3.1 No Parameter (). 
3.2 Single Parameter.
3.3 Multiple Parameters ().
3.4 Multi Line
4. Destructuring: Object Destructuring and Array Destructuring.
5. Object Declaration Shorthand.
6. Function parameter Default Value
7. Optional Chaining (?.)

==========================
==============

Browser API
1. Local Storage and Session Storage
2. Location API 
3. History API.
4.Fetch

=========================
==============

Others 

1. Array: map, ForEach, Filter, Find,
2. Ternary Operator.
3. Logical and logical Or. && , ||
4. JSON (Stringify, Parse);
5. +, '',

*/

const x = 500;
const y = 100;
const obj = {
  x: x,
  y: y
};

// shortchut 
const obj1 = {x, y};

const name = 'Solimullah';
const area = 'Dhaka';

const nobab = {
  name: name,
  area: area,
};

const nobab2 = {name, area}